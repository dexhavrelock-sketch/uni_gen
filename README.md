# uni_gen
A simple Python library for generating unique codes in binary or decimal.

## Installation
`pip install git+https://github.com/dexhavrelock-sketch/uni_gen.git`

## Usage
```python
import uni_gen
uni_gen.uni(10, 1, "my_codes") # Generates 10 binary codes
